var express = require('express');
var router = express.Router();
var url = require('url');
var querystring = require('querystring');
var addmysql = require('../modules/add');
router.get('/add',function(req,res){

	//res.render('add',{title:'users'});
})
module.exports = router;